# Dansa-MediaPlayer
Dansa is a Windows-based media player built with WPF and a custom FFmpeg decoding pipeline. It features hardware-accelerated video playback (DXVA2 / D3D11), support for modern codecs including AV1, playlist-driven playback, subtitle rendering, frame-accurate seeking, and extensible rendering and audio pipelines.
